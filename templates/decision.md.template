# NNNN — <短标题，小写横线连接>

## Status（状态）
YYYY-MM-DD — proposed | implemented | rejected — <一句话理由>

## Context（背景）

什么问题或机会驱动了这个决策。要写得脱离方案也能读懂 —— 读者应该即使跳过决策也能理解压力。

包括：

- 可观察的症状或约束
- 为什么现有模式解决不了
- 截止时间或触发器（如果有）

## Decision（决策）

我们决定的内容。现在时、事实性。决策要让一个新 Agent 能验证代码和它对齐而不含糊。

例如：

- "把会话日志以 JSONL 存在 `<configDir>/sessions/session-<id>.jsonl[.zstd]`。"
- "关系数据用 SQLite，通过 `better-sqlite3` 访问。"
- "拒绝碰 `vendor/` 的 PR。"

## Consequences（影响）

- ✅ **得到：** <这条决策给我们什么>
- ❌ **付出：** <我们放弃什么或为什么付出>
- ⚠️ **工作流变化：** <我们的日常工作怎么变>

## Alternatives considered（考虑过的方案）

**必填**。每个真实方案给一段：

- **<方案 A>：** <拒绝原因 —— 一到三句话>
- **<方案 B>：** <拒绝原因>
- **<方案 C>：** <拒绝原因>

**规则：方案必须是真的。** 不要编稻草人。如果你没法从记录里重建这个方案，标这一节：

```markdown
<!-- alternatives-not-recorded (pre-format or informal decision) -->
```

## Lifecycle（生命周期）

文件随决策状态变化移动：

- `proposed/NNNN-*.md` → 评审中，代码可能存在也可能不存在
- `implemented/NNNN-*.md` → 已上线，代码和这文件一致，事实保持同步
- `rejected/NNNN-*.md` → 考虑过但拒绝，只在能防止合理错误时保留

移动时，更新 `Status:` 行，并把 `## Decision` 从未来时（提案）改写成现在时（已上线）。